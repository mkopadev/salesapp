using NUnit.Framework;

namespace SalesApp.Core.Tests.Services.Person.Device
{
    [TestFixture]
    public class InformationTest
    {
        [Test]
        public void TestVersioning()
        {
            // Assert.That(new Information().Major, Is.EqualTo(1));
            // Assert.That(Information.Minor, Is.EqualTo(0));
            // Assert.That(Information.Build, Is.Positive);
            // Assert.That(Information.Revision, Is.Positive);
        }
    }
}